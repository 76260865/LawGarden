<?xml version="1.0" encoding="UTF-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="#F7FAF8"
    android:focusable="true"
    android:focusableInTouchMode="true"
    android:orientation="vertical" >

    <RelativeLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/ic_menu_bg" >

        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_marginLeft="20dip"
            android:src="@drawable/top_logo" />

        <RadioGroup
            android:id="@+id/rgp_bottom"
            android:layout_width="250dip"
            android:layout_height="wrap_content"
            android:layout_alignParentRight="true"
            android:gravity="center"
            android:orientation="horizontal" >

            <RadioButton
                android:id="@+id/rbtn_law_data"
                style="@style/RadioButton"
                android:drawableTop="@drawable/ic_menu_law_data"
                android:paddingTop="10dip" />

            <RadioButton
                android:id="@+id/rbtn_law_news"
                style="@style/RadioButton"
                android:drawableTop="@drawable/ic_menu_news"
                android:paddingTop="10dip" />

            <RadioButton
                android:id="@+id/rbtn_law_fav"
                style="@style/RadioButton"
                android:drawableTop="@drawable/ic_menu_icon_fav"
                android:paddingTop="10dip" />

            <RadioButton
                android:id="@+id/rbtn_law_user"
                style="@style/RadioButton"
                android:drawableTop="@drawable/ic_menu_user"
                android:paddingTop="10dip" />
        </RadioGroup>
    </RelativeLayout>

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="0dip"
        android:layout_weight="1.0"
        android:orientation="horizontal" >

        <FrameLayout
            android:id="@+id/fragment_container"
            android:layout_width="0dip"
            android:layout_height="fill_parent"
            android:layout_weight="1" >
        </FrameLayout>

        <View
            android:layout_width="1dip"
            android:layout_height="fill_parent"
            android:background="#dedede" />

        <fragment
            android:id="@+id/fragment_detail_article_list"
            android:name="com.jason.lawgarden.ArticleListFragment"
            android:layout_width="0dip"
            android:layout_height="fill_parent"
            android:layout_weight="2" />

        <fragment
            android:id="@+id/fragment_detail_article"
            android:name="com.jason.lawgarden.ArticleFragement"
            android:layout_width="0dip"
            android:layout_height="fill_parent"
            android:layout_weight="2" />
        <!-- need use articlefragment, articlelistfragement and newsfragment favoritefragemt... -->
    </LinearLayout>

</LinearLayout>